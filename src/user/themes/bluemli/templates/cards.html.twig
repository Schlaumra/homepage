{% extends 'partials/base.html.twig' %}

{% block content %}
    <div class="container">
    {% for module in page.collection() %}
        <div class="columns">
            <div class="column col-12">
            {{ module.content|raw }}
            </div>
        </div>
    {% endfor %}
    </div>
{% endblock %}